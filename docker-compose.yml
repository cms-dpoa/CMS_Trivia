version: "3.3"

services:
  frontend:
    image: view_cms_game
    depends_on:
      - backend
    ports:
      - "80:80"

        # environment:
      #   MONGO_URL: "django://api:8000/"
    # volumen:
    #   - view:/view/build

  backend:
    image: api_cms_game
    ports:
      - "8000:8000"

    # command: python manage.py runserver 0.0.0.0:8000
  # nginx:
  #   image: nginx:1.19.0-alpin
  #   ports:
  #     - 80:80
